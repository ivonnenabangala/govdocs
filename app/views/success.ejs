<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Upload Success</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light d-flex justify-content-center align-items-center vh-100">
  <div class="card shadow p-4 text-center" style="max-width: 400px;">
    <h3 class="text-success mb-3">✅ Document Uploaded Successfully!</h3>
    <p class="text-muted">Your document "<%= title %>" was uploaded.</p>
    <p class="text-secondary">Redirecting to homepage in <span id="countdown">5</span> seconds...</p>

    <a href="/" class="btn btn-primary mt-3">Go Now</a>
  </div>

  <script>
    let seconds = 5;
    const countdown = document.getElementById('countdown');
    const interval = setInterval(() => {
      seconds--;
      countdown.textContent = seconds;
      if (seconds <= 0) {
        clearInterval(interval);
        window.location.href = '/';
      }
    }, 1000);
  </script>
</body>
</html>
